x = 0.001
e = 0.0001
high = x
low = 0
ans = (low + high) / 2.0

while abs(ans**2 - x) > e:

	if ans**2 - x > e:
		high = ans
	
	elif ans**2 - x < e:
	
		low = ans
	
	ans = (low + high) / 2.0
	
print(str(ans) + 'is close to the square root of ' + str(x))